{
	"name": "quintus-monorepo",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@10.28.1",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "pnpm --recursive --sort run --if-present build",
		"test": "vitest run",
		"test:e2e": "vitest run --config packages/create-quintus2/vitest.e2e.config.ts",
		"test:watch": "vitest --watch",
		"test:coverage": "vitest run --coverage",
		"lint": "biome check .",
		"lint:fix": "biome check . --write",
		"dev": "vite examples",
		"docs": "typedoc",
		"test:platformer": "vitest run --config examples/platformer/vitest.config.ts",
		"test:dungeon": "vitest run --config examples/dungeon/vitest.config.ts",
		"build:examples": "pnpm build && rm -rf examples/dist && pnpm exec vite build examples",
		"clean": "pnpm --recursive exec rm -rf dist",
		"preinstall": "npx only-allow pnpm",
		"release": "node scripts/release.mjs",
		"sync:templates": "node scripts/sync-template-tooling.mjs",
		"qdbg": "./bin/qdbg"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.0",
		"@vitest/coverage-v8": "^3.0.0",
		"jsdom": "^25.0.0",
		"sharp": "^0.34.5",
		"tsup": "^8.0.0",
		"typedoc": "^0.28.20",
		"typescript": "^5.7.0",
		"vite": "^6.0.0",
		"vitest": "^3.0.0",
		"vitest-canvas-mock": "^1.1.3"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"esbuild"
		]
	}
}
